## Question: Visualizing Train and Test Predictions

In the Session 1 code, the same learned regression line is plotted on both the training set and the test set.

1. Why should the line be the same in both plots?
2. What changes between the training plot and the test plot?
3. Draw two side-by-side plots:
   - training set scatter plot with regression line,
   - test set scatter plot with the same regression line.
4. If the line fits training data very well but test data very poorly, what might be happening?
5. If the line fits both training and test data poorly, what might be happening?
6. How does this connect to underfitting and overfitting?
7. Explain why visualizations are useful even when we already have MSE numbers.

## Question: Prediction Story

A model learns:

$$
\hat{y}=9000x+25000
$$

where $x$ is years of experience and $\hat{y}$ is predicted salary.

1. Interpret the slope.
2. Interpret the intercept.
3. Predict the salary for $x=3$.
