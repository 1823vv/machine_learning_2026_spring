## Question: Regression Metrics — MSE, MAE, and $R^2$

For regression, suppose the true targets are $y^{(i)}$, predictions are $\hat y^{(i)}$, and there are $n$ examples.

1. Write the mean squared error formula and the mean absolute error formula. Explain why MSE penalizes large errors more strongly than MAE.
2. Write the $R^2$ formula or explain its meaning in words.
