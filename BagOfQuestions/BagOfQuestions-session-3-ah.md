## Question: Universal Approximation Theorem

Neural networks with nonlinear activation functions can approximate very complicated functions when they have enough hidden units.

1. State the universal approximation theorem in one or two sentences.
2. Explain why the theorem depends on using nonlinear activation functions rather than only stacked linear layers.
3. Draw a simple schema showing input features, a hidden layer with nonlinear activations, and an output approximating a target function.
